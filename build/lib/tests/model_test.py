from conv_rbg.models import ConvRGB


def test_create_model():
    """
    GIVEN ConvRGB with
    WHEN the class is instantiated is called
    THEN a new Article must exist in the database with the same attributes
    """
    assert True
